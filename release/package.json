{
  "name": "release",
  "version": "1.0.0",
  "description": "There are currently two options for running the server: through firebase and through the standalone localserver.js",
  "main": "localserver.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mysql": "^2.18.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
